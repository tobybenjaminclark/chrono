transition_alpha = 1;     // start fully black if entering a room
transition_speed = 0.05;  // adjust for slower/faster fade
transition_state = "in";  // "in" = fade from black → visible
next_room = -1;
